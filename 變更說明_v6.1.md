# SlotPlanner Pro — v6.1 變更說明（版面 / 效能 / 一致性）

> 本版聚焦三件事：**修掉窄螢幕的版面擠壓**、**降低永久背景的效能負擔**、**補上 v5.3 漏掉的賠付表一致性**。
> 全程以無頭瀏覽器於 1440 / 1180 / 1024px 實際渲染驗證，巡覽全部分頁 0 JS error。

執行方式不變（必須用 HTTP server）：
```
cd slotplanner
python -m http.server 8000
# 開 http://localhost:8000/app.html
```

---

## A. 版面修正（RWD / CJK 擠壓）

改檔：`css/theme_additions.css`（檔尾新增 v6.1 批次）、`js/config-editor/setup.js`、
`js/config-editor/template.js`

1. **Test Inspector 浮動面板不再遮擋內容**
   - 之前：進入「01_Global / 09 規則」時面板強制展開、固定在右下角，蓋住
     「新增模式 / JP 定義」與「觸發點 / 觸發條件」等控制項（1024px 幾乎不能操作）。
   - 之後：預設收合成角落小標頭；自動 pin 保持收合（不彈開），手動「釘到 Test
     Inspector」才展開，取消釘住自動收回。功能完全保留，只是改為「需要時才開」。
   - 機制：`pinTest(kind,id,label, autoOpen)` 新增 `autoOpen` 參數；`_autoPinIfNeeded`
     一律以 `autoOpen=false` 呼叫；`inspectorOpen` 預設 `false`、`unpinTest` 自動收回。

2. **01_Global 雙欄不再在 ~1024px 被壓成單欄 chips**
   - 根因：`.cfg-global-grid` 同時有三處互相衝突的定義（其中「1fr 1fr / 900px 才折」
     的版本勝出，導致 1024px 仍硬塞兩欄、左欄壓到一排只放得下一顆 chip）。
   - 修正：統一為單一來源（左欄略寬、**1100px 以下折成單欄**），並中和失效的
     `flex` 死碼。賠付類型 / 計分方向 chips 正常換行。

3. **Symbol 編輯器標題列不再把按鈕文字擠成直排**
   - 「匯出 JSON / 匯入 JSON / 重設 / 啟用中」加 `white-space:nowrap`；標題列允許整列
     換行、高度自適應，窄欄時整顆按鈕換行而非裁切或標題消失。

4. **06_Paylines「方向 左→右 於…調整」群組不再逐字直排**
   - 原本該群組無任何 CSS；改為 `inline-flex + nowrap`，工具列過窄時整組換行。

---

## B. 效能優化

改檔：`css/theme.css`；移除死檔。

1. **馴服永久背景動畫**（最大體感改善）
   - `body::before` 的 `bgFloat`：獨立合成圖層（`will-change:transform`）+ 動畫
     32s→60s + 位移/縮放幅度減半。大幅降低其下 `card-shell` backdrop-filter 每幀重採樣。
2. **降低全幅玻璃模糊**：`--blur-thick` 40px→30px（card-shell 永久全幅，視覺幾乎無差）。
3. **移除已不載入的死檔**：`js/slotplanner.js`（v4.9-b 起未載入）、
   `css/modules/sim-page.css`（v4.9-b 起未 @import）。

> 視覺風格（iOS 玻璃）維持不變，已渲染比對確認。

---

## C. 賠付表一致性（補 v5.3 漏網）

改檔：`js/symbol.js`、`js/registry.js`

- 之前：基礎建設（`SP.migratePayRows`、`.sym-pay-dynamic` CSS、`03c_Paytable` 匯出）
  早已就位，但 `symbol.js` 仍用固定 4 格（pay_3x–6x），且 `registry.js` 在
  `cloneSymbol / toJSON / fromJSON / _loadFromLocalStorage` 四處的欄位白名單未含
  `pay_rows`，會在每次存檔被丟棄。
- 之後：
  - 符號編輯器改用**動態賠付表**（可加/刪列，支援 2–20 連），以 `form.pay_rows` 為主；
    存檔時同步回填 `pay_3x–6x` 維持向下相容。
  - `registry.js` 五處（含預設）補上 `pay_rows`，持久化不再遺失（含 7 連等非 3/4/5/6 列）。
  - 舊資料（無 `pay_rows`）載入時自動由 `pay_Nx` 遷移；匯出 `03c_Paytable` 原本就讀
    `pay_rows`（缺則回退 `pay_Nx`），故新舊資料皆正確。

---

## 變動檔案總覽

| 檔案 | 變動 |
|---|---|
| `css/theme.css` | 背景動畫減負、`--blur-thick` 40→30 |
| `css/theme_additions.css` | 檔尾新增 v6.1 版面修正批次 + 可收合分頁列 CSS |
| `js/config-editor/setup.js` | Inspector 預設收合 / autoOpen 參數 / 分頁列收合狀態 |
| `js/config-editor/template.js` | 分頁列收合鈕 + tab hover 標題 |
| `js/symbol.js` | 動態 pay_rows UI + 讀寫同步 |
| `js/registry.js` | 五處補 `pay_rows` 持久化 |
| （移除）`js/slotplanner.js`、`css/modules/sim-page.css` | 死檔清除 |

## 額外 UX（v6.1）
- 設定檔編輯器分頁列可一鍵 **「« 收合分頁列」**（220px↔56px，記憶於本機），
  窄螢幕替編輯區釋放約 164px 橫向空間。
